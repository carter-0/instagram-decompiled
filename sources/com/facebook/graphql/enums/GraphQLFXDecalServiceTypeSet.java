package com.facebook.graphql.enums;

import X.0sc;
import java.util.HashSet;
import java.util.Set;

public final class GraphQLFXDecalServiceTypeSet {
    public static final HashSet A00 = 0sc.A05(new String[]{"CROSS_POSTING", "FBPAY", "MANAGE_ACCOUNTS", "MESSAGING_INTEROP", "META_VERIFIED", "SHARED_PROFILES_CROSS_POSTING", "SHARED_PROFILES_MANAGE_ACCOUNTS", "SSO", "WA_CROSS_POSTING"});

    public static final Set getSet() {
        return A00;
    }
}
